# For local dev (SQLite — no Docker required)
DATABASE_URL=sqlite+aiosqlite:///./simtrade.db

# For production (PostgreSQL via Docker)
# DATABASE_URL=postgresql+asyncpg://simtrade:simtrade@localhost:5432/simtrade

ADMIN_API_KEY=synthex-god-mode-secret
